[project]
name = "timescaledb-mock-data"
version = "0.1.0"
description = "Generate mock weather data for TimescaleDB"
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9.9",
    "numpy>=1.26.0",
]

[tool.uv]
dev-dependencies = []
